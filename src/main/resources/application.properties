spring.application.name=weather-dashboard

#weather.api.url = ${WEATHER_API_URL}
#weather.api.key = ${WEATHER_API_TOKEN}
#
#spring.datasource.url=${SPRING_DATASOURCE_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

 spring.datasource.url=jdbc:postgresql://localhost:5432/weather_db
 spring.datasource.username=postgres
 spring.datasource.password=177013
 spring.jpa.hibernate.ddl-auto=update

 weather.api.url = https://api.weatherapi.com/v1/current.json
 weather.api.key = 6cdd0449d15e488a9f792119250312

# name: CI/CD Pipeline

# on:
#   push:
#     branches: [ "main" ]

# env:
#   # This tells GitHub to use the latest Node engine to avoid warnings
#   FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true 

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Set up Java 21
#         uses: actions/setup-java@v4
#         with:
#           java-version: '21'
#           distribution: 'temurin'
#           cache: 'maven'

#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build and Push Image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           push: true
#           tags: ${{ secrets.DOCKER_USERNAME }}/my-spring-app:latest

#   deploy-to-railway:
#     needs: build-and-push
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4
        
#       - name: Install Railway CLI
#         run: npm install -g @railway/cli

#       - name: Deploy to Railway
#         run: railway upsert --detach
#         env:
#           RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

# FROM maven:3.9.6-eclipse-temurin-21-alpine AS build
# COPY . .
# RUN mvn clean package -DskipTests

# FROM eclipse-temurin:21-jdk-alpine
# COPY --from=build /target/*.jar app.jar
# EXPOSE 8080
# ENTRYPOINT ["java","-jar","/app.jar"]